const Logo = ({ width, height }) => {
  return (
    <a href="https://chaicode.com/" target="_blank">
      <img src="/logo.png" alt="chai aur code" width={width} height={height} />
    </a>
  );
};

export default Logo;
